bundle:
  name: datacart-storefront

include:
  - resources/*.yml

variables:
  lakebase_project:
    description: "Lakebase project name (e.g., lakebase-branching-workshop-<username>)"
  endpoint_name:
    description: "Full Lakebase endpoint path (e.g., projects/<project>/branches/production/endpoints/primary)"
  db_schema:
    default: "ecommerce"

targets:
  dev:
    default: true
    mode: development
    workspace:
      profile: <your-profile>    # ← Change to your Databricks CLI profile (run `databricks auth profiles`)

  workshop:
    mode: production
    workspace:
      profile: <your-profile>    # ← Change to your Databricks CLI profile (run `databricks auth profiles`)
